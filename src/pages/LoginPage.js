import React from 'react';
import Header from '../components/Header';

const LoginPage = () => {
    return (
        <div>
            <Header title="로그인" />
        </div>
    );
};

export default LoginPage;